/* tslint:disable */
// generated by typescript-json-validator
import {inspect} from 'util';
import Ajv = require('ajv');
import {UserProps} from './UserProps';
export const ajv = new Ajv({"allErrors":true,"coerceTypes":false,"format":"fast","nullable":true,"unicode":true,"uniqueItems":true,"useDefaults":true});

ajv.addMetaSchema(require('ajv/lib/refs/json-schema-draft-06.json'));

export {UserProps};
export const UserPropsSchema = {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "defaultProperties": [
  ],
  "properties": {
    "age": {
      "type": "number"
    },
    "id": {
      "type": "number"
    },
    "name": {
      "type": "string"
    }
  },
  "type": "object"
};
export type ValidateFunction<T> = ((data: unknown) => data is T) & Pick<Ajv.ValidateFunction, 'errors'>
export const isUserProps = ajv.compile(UserPropsSchema) as ValidateFunction<UserProps>;
export default function validate(value: unknown): UserProps {
  if (isUserProps(value)) {
    return value;
  } else {
    throw new Error(
      ajv.errorsText(isUserProps.errors!.filter((e: any) => e.keyword !== 'if'), {dataVar: 'UserProps'}) +
      '\n\n' +
      inspect(value),
    );
  }
}
